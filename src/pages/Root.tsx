const Root = () => {
  return (
    <div
      style={{ display: 'flex', flexDirection: 'column', alignItems: 'center' }}
    >
      <h1>root</h1>
      <button>about</button>
    </div>
  );
};

export default Root;
